import React, {useState} from 'react';
import {View, Text, StyleSheet, SafeAreaView, TextInput, TouchableOpacity, Alert, Button} from 'react-native';
import Ocorrencia from '../../../App';
export default function Conclusao({changeStatus}){
  
    const Conclusao = () => {
return(
  <SafeAreaView style={styles.container}>
    <Button
          title="Back to home"
          onPress={() =>
            this.props.navigation.navigate('Ocorrencia')
          }
        />

  </SafeAreaView>
);
    };
}